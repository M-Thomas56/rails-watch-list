<h2>Add a new list</h2>
  <p>--------------------------------------------------</p>
<%= form_with model: @list do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>
